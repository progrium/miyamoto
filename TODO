high pri:
-reschedule replicas on dispatch
-dispatch response, task finishing
-idempotency
 -local node enqueue idempotency
 -internal uniqueness check?
 -worker fail on duplicate
-functional tests
-automated cluster performance tests
 -clusterable enqueuer and dequeuer samplers
 
low pri:
-precomputed uuid cache/generator
-tcp keepalives instead of app keepalives for cluster manager
-zmq enqueue interface
-normalize to json/dict task structure
-rate limiting
-error webhook